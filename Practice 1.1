//Java practice using (double) as a polygon area calcultator

public class MyClass {
    
    public static void main(String args[]) {
        
    double lado=10.5;
    double apotema=8.3;
    int nl=11;
    double p= nl*lado;
    double a= (p*apotema)/2;
    
    
        System.out.println("El Area del Poligono es = " + a);
    }
}
